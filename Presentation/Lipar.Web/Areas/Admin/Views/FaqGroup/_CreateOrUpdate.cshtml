@model FaqGroupModel

<input type="hidden" asp-for="Id" />

<!--load info faq group-->
@await Html.PartialAsync("_CreateOrUpdate.Info", Model)

@if (Model.Id > 0)
{
    <!--load list faq-->
    @await Html.PartialAsync("_CreateOrUpdate.FaqList", Model)
}