@model CommonSettingModel

@{
    //Html.SetActiveMenuItemSystemName("Blog List");
    ViewBag.PageTitle = T("Admin.CommonSetting.Edit.Card.Title").Text;
}

<form asp-action="Common" asp-controller="Setting" method="post">
    <section class="card">
        <div class="card-header clearfix">
            <div class="pull-right">
                <span class="card-title">@T("Admin.CommonSetting.Card.Title")</span>
            </div>

            <div class="pull-left">
                <button type="submit" class="btn btn-success">
                    <i class="fa fa-floppy-o"></i>
                    @T("Common.Edit.Button")
                </button>
                <a asp-action="List" class="btn btn-primary">
                    <i class="fa fa-arrow-circle-left"></i>
                    @T("Common.Back.Button")
                </a>
            </div>
        </div>
        <div class="card-body">
            @await Html.PartialAsync("_CreateOrUpdate.Common", Model)
        </div>
    </section>
</form>

