@model DynamicPageDetailModel
@{
    Html.SetActiveMenuItemSystemName("Dynamic Page List");
    ViewBag.PageTitle = T("Admin.DynamicPageDetail.Add.Card.Title").Text;
}

<form asp-action="DynamicPageDetailCreate" asp-controller="DynamicPage" method="post">
    <section class="card">
        <div class="card-header clearfix">
            <div class="pull-right">
                <span class="card-title">@T("Admin.DynamicPageDetail.Add.Card.Title")</span>
            </div>

            <div class="pull-left">
                <button type="submit" class="btn btn-success">
                    <i class="fa fa-floppy-o"></i>
                    @T("Common.Add.Button")
                </button>
                <a asp-action="DynamicPageDetails" asp-route-filterByPageId="@Model.DynamicPageId" class="btn btn-primary">
                    <i class="fa fa-arrow-circle-left"></i>
                    @T("Common.Back.Button")
                </a>
            </div>
        </div>
        <div class="card-body">
            @await Html.PartialAsync("_CreateOrUpdate.DynamicPageDetail", Model)
        </div>
    </section>

    @Html.AntiForgeryToken()
</form>