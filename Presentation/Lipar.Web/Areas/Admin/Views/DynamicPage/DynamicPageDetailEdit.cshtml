@model DynamicPageDetailModel
@{
    Html.SetActiveMenuItemSystemName("Dynamic Page List");
    ViewBag.PageTitle = T("Admin.DynamicPageDetail.Edit.Card.Title").Text;
}

<form asp-action="DynamicPageDetailEdit" asp-controller="DynamicPage" method="post">
    <section class="card">
        <div class="card-header clearfix">
            <div class="float-left">
                <span class="card-title">@T("Admin.DynamicPageDetail.Edit.Card.Title") - @Model.Name</span>
            </div>

            <div class="float-right">
                <button type="submit" class="btn btn-success">
                    <i class="fa fa-floppy-o"></i>
                    @T("Common.Edit.Button")
                </button>
                <span id="dynamic-page-detail-delete" class="btn btn-danger">
                    <i class="fa fa-trash-o"></i>
                    @T("Common.Delete.Button")
                </span>
                <a asp-action="DynamicPageDetails" asp-route-filterByPageId="@Model.DynamicPageId" class="btn btn-primary">
                    <i class="fa fa-arrow-circle-left"></i>
                    @T("Common.Back.Button")
                </a>
            </div>
        </div>
        <div class="card-body">
            @await Html.PartialAsync("_CreateOrUpdate.DynamicPageDetail", Model)
        </div>
    </section>

    @Html.AntiForgeryToken()
</form>


<lipar-delete-confirmation asp-button-id="dynamic-page-detail-delete" asp-model-id="@Model.Id" />