@model DeliveryDateModel
@{
    Html.SetActiveMenuItemSystemName("Delivery Date List");
    ViewBag.PageTitle = T("Admin.DeliveryDate.Edit.Card.Title").Text;
}

<form asp-action="Edit" asp-controller="DeliveryDate" method="post">
    <section class="card">
        <div class="card-header clearfix">
            <div class="float-left">
                <span class="card-title">@T("Admin.DeliveryDate.Edit.Card.Title") - @Model.Name</span>
            </div>

            <div class="float-right">
                <button type="submit" class="btn btn-success">
                    <i class="fa fa-floppy-o"></i>
                    @T("Common.Edit.Button")
                </button>
                <span id="delivery-date-delete" class="btn btn-danger">
                    <i class="fa fa-trash-o"></i>
                    @T("Common.Delete.Button")
                </span>
                <a asp-action="List" class="btn btn-primary">
                    <i class="fa fa-arrow-circle-left"></i>
                    @T("Common.Back.Button")
                </a>
            </div>
        </div>
        <div class="card-body">
            @await Html.PartialAsync("_CreateOrUpdate", Model)
        </div>
    </section>

    @Html.AntiForgeryToken()
</form>

<lipar-delete-confirmation asp-button-id="delivery-date-delete" asp-model-id="@Model.Id" />