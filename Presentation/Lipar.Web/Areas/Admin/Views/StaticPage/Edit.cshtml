@model StaticPageModel
@{
    Html.SetActiveMenuItemSystemName("Static Page List");
    ViewBag.PageTitle = T("Admin.StaticPage.Edit.Card.Title").Text;
}

<form asp-action="Edit" asp-controller="StaticPage" method="post">
    <section class="card">
        <div class="card-header clearfix">
            <div class="float-left">
                <span class="card-title">@T("Admin.StaticPage.Edit.Card.Title") - @Model.Name</span>
            </div>

            <div class="float-right">
                <button type="submit" class="btn btn-success">
                    <i class="fa fa-floppy-o"></i>
                    @T("Admin.Common.Edit")
                </button>
                <span id="static-page-delete" class="btn btn-danger">
                    <i class="fa fa-trash-o"></i>
                    @T("Admin.Common.Delete")
                </span>
                <a asp-action="List" class="btn btn-primary">
                    <i class="fa fa-arrow-circle-left"></i>
                    @T("Admin.Common.Back")
                </a>
            </div>
        </div>
        <div class="card-body">
            @await Html.PartialAsync("_CreateOrUpdate", Model)
        </div>
    </section>

    @Html.AntiForgeryToken()
</form>

<lipar-delete-confirmation asp-button-id="static-page-delete" asp-model-id="@Model.Id" />